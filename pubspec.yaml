name: increment_decrement_app
description: A simple Flutter app that demonstrates increment and decrement through good UI. xD

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  fonts:
    - family: Saadin
      fonts:
        - asset: fonts/BebasNeueRegular.ttf